# Core
numpy>=2.0.0,<2.3  # upgraded from 1.x - robosuite 1.5/robomimic 0.4 work with numpy 2.x
torch>=2.10.0
hydra-core>=1.3.2
wandb>=0.24.0

# Vision & Transforms
transformers>=4.53.0
opencv-python>=4.11.0
pillow>=12.0.0
einops>=0.8.0
imageio>=2.36.0
imageio-ffmpeg>=0.5.0
matplotlib>=3.10.0

# Simulation
mujoco>=3.7.0
robosuite>=1.5.0  # LIBERO updated for 1.5.x API (composite controllers, etc.)
robomimic @ git+https://github.com/ARISE-Initiative/robomimic.git@v0.4.0  # v0.4+ has robosuite 1.5 compat
bddl>=3.6.0
gymnasium>=1.0.0  # modern replacement for gym
mink>=1.0.0  # numpy 2.x compatible (robosuite pins 0.0.5 but 1.x works)

# OpenPI integration
jax[cuda12]>=0.10.0
flax>=0.12.0
safetensors>=0.7.0
tyro>=1.0.0

# Utils
h5py>=3.16.0
cloudpickle>=3.1.0
tqdm>=4.67.0
tensorboard>=2.20.0
psutil>=7.2.0
termcolor>=3.3.0
